Sync Anki flashcards with a markdown file.  Accesses the Anki database directly, no additional plugins required.

The profile `flashimp` has to exist in Anki beforehand:

```
$ ./flashimp.sh flashcards.md
```

# Design

 * One file, one Anki deck
